<UserControl x:Class="EasyRooms.View.OptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0">
            <Label Margin="7, 0, 0, 0" Content="Rooms"/>
            <TextBox MinHeight="200" Margin="10, 0, 10, 0" AcceptsReturn="True" Text="{Binding RoomsString}"/>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1">
            <Label Margin="7, 0, 0, 0" Content="PartnerRooms" />
            <TextBox MinHeight="200" Margin="10, 0, 10, 0" AcceptsReturn="True" Text="{Binding PartnerRoomsString}"/>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="2">
            <Label Margin="7, 0, 0, 0" Content="MassagesForSpecificRooms" />
            <TextBox MinHeight="200" Margin="10, 0, 10, 0" AcceptsReturn="True" Text="{Binding MassagesForSpecificRooms}"/>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="3">
            <Label Margin="7, 0, 0, 0" Content="RoomsForSpecificMassages"/>
            <TextBox MinHeight="200" Margin="10, 0, 10, 0" AcceptsReturn="True" Text="{Binding RoomsForSpecificMassages}"/>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="4">
            <Label Margin="7, 0, 0, 0" Content="StartTime" />
            <TextBox Margin="10, 0, 10, 0" Text="{Binding StartTime}" />
            <Label Margin="7, 0, 0, 0" Content="EndTime" />
            <TextBox Margin="10, 0, 10, 0" Text="{Binding EndTime}" />
            <Label Margin="7, 0, 0, 0" Content="Buffer"/>
            <TextBox Margin="10, 0, 10, 0" Text="{Binding Buffer}"/>
        </StackPanel>

        <Button Grid.Column="4" Grid.Row="1" Command="{Binding SaveRoomsCommand}" Content="Räume Speichern"
                VerticalAlignment="Center" HorizontalAlignment="Center"
                MinWidth="120" />
    </Grid>
</UserControl>
